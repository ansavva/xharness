.DS_Store
.vscode/
__pycache__/
*.pyc
.claude/settings.local.json

# Secrets — real values only; .env.example is committed
.env
# Google OAuth client secrets (Desktop app JSON) + any cached token
credentials.json
token.json

# Generated video output (seedance-video skill)
/output/

# Terraform (infra/) — local state, cached providers, plan files
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
*.tfvars.local
